<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MadhumitasAndroidLabs" parent="Theme.Material3.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/my_primary</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/my_secondary</item>

        <!-- Statusbar brand color. -->
        <item name="android:statusBarColor">@color/my_statusbar</item>
        <!-- Customize your theme here -->
        <item name="android:textColor">@color/my_text</item>
    </style>



    <style name="Theme.MadhumitasAndroidLabs" parent="Base.Theme.MadhumitasAndroidLabs" />
</resources>